{
  "name": "questionnaire-backend",
  "version": "1.0.0",
  "description": "Backend for questionnaire web application",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "NODE_ENV=production node server.js",
    "dev": "node --watch server.js",
    "build": "cd ../frontend && npm install && npm run build && cp -r dist ../backend/public"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sql.js": "^1.10.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5"
  }
}
